name: 'PR Checks'

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
      - '!changeset-release*'

jobs:
  pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@ac593985615ec2ede58e132d2e21d2b1cbd6127c # v3

      - name: Enable node
        uses: actions/setup-node@master
        with:
          node-version: 18.x

      - name: Install dependencies
        run: |
          npm install -g yarn
          yarn install

      - name: Build packages 🏗️
        run: yarn build

      - name: Validate ✅
        run: yarn validate

      - name: Danger
        uses: danger/danger-js@647e3c1bc7b01c447e64f7fe77577e2b28fc1887 # 11.2.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
