export const metaTitle = 'Paragraph'
export const description = 'Paragraph component of DarenUI'
export const pkg = 'typography'
export const path = 'paragraph'

# Paragraph

Paragraph is a text element. It is used to display a paragraph of text.

<Code code={`import { Paragraph } from '@daren/ui-components'ˇˇ`} />

## Preview

<Preview
  code={`<Paragraph>
  Once upon a time, there was a React component named SelfTest. SelfTest was
  special because it had the ability to test itself. Every time the
  component rendered, it would run a series of tests to ensure that it was
  working properly. The tests were thorough and covered all of the
  component's functionality. SelfTest was proud of its ability to test
  itself and never failed a single test.
</Paragraph>`}
>
  <div className="space-y-2">
    <Paragraph>
      Once upon a time, there was a React component named SelfTest. SelfTest was
      special because it had the ability to test itself. Every time the
      component rendered, it would run a series of tests to ensure that it was
      working properly. The tests were thorough and covered all of the
      component's functionality. SelfTest was proud of its ability to test
      itself and never failed a single test.
    </Paragraph>
  </div>
</Preview>

---

## Properties

<Properties>
  <Property name="variant" type="[primary, secondary]">
    The variant of the paragraph.
  </Property>
  <Property name="children" type="React.ReactNode" required>
    The content of the paragraph.
  </Property>
</Properties>

---

### Variant

There are some default styles for the paragraph. You can change the variant to
change the style.

<Preview
  code={`<Paragraph variant="primary">
  Once upon a time, there was a React component named SelfTest. SelfTest was
  special because it had the ability to test itself. Every time the
  component rendered, it would run a series of tests to ensure that it was
  working properly. The tests were thorough and covered all of the
  component's functionality. SelfTest was proud of its ability to test
  itself and never failed a single test.
</Paragraph>
<Paragraph variant="secondary">
  Once upon a time, there was a React component named SelfTest. SelfTest was
  special because it had the ability to test itself. Every time the
  component rendered, it would run a series of tests to ensure that it was
  working properly. The tests were thorough and covered all of the
  component's functionality. SelfTest was proud of its ability to test
  itself and never failed a single test.
</Paragraph>`}
>
  <div className="flex flex-col">
    <Paragraph variant="primary">
      Once upon a time, there was a React component named SelfTest. SelfTest was
      special because it had the ability to test itself. Every time the
      component rendered, it would run a series of tests to ensure that it was
      working properly. The tests were thorough and covered all of the
      component's functionality. SelfTest was proud of its ability to test
      itself and never failed a single test.
    </Paragraph>
    <Paragraph variant="secondary">
      Once upon a time, there was a React component named SelfTest. SelfTest was
      special because it had the ability to test itself. Every time the
      component rendered, it would run a series of tests to ensure that it was
      working properly. The tests were thorough and covered all of the
      component's functionality. SelfTest was proud of its ability to test
      itself and never failed a single test.
    </Paragraph>
  </div>
</Preview>
