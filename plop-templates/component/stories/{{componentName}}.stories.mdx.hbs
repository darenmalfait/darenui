import { {{pascalCase componentName}} } from "../lib";
import { Actions } from "../../../storybook/components/actions";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

<Meta
  title="Layout Components/{{pascalCase componentName}}"
  component={{{pascalCase componentName}}}
  argTypes={{}}
/>

export const Template = (args) => (
  <{{pascalCase componentName}} {...args} />
);

# {{pascalCase componentName}}

`{{pascalCase componentName}}` is ...

<Actions packageName="{{componentName}}" />

## Import

```bash
import { {{pascalCase componentName}} } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
