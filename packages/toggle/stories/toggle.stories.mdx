import { Toggle } from "../lib";
import { Actions } from "../../../storybook/components/actions";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

<Meta
  title="Components/Toggle"
  component={Toggle}
  argTypes={{
    id: {
      description: "The id of the toggle.",
      defaultValue: "unique-id",
      control: {
        type: "text",
      },
    },
    onChange: { action: "clicked" },
    activeColorClass: {
      description: "The class name of the active color.",
      defaultValue: "bg-green-300",
      control: {
        type: "text",
      },
    },
    inactiveColorClass: {
      description: "The class name of the inactive color.",
      defaultValue: "bg-gray-200",
      control: {
        type: "text",
      },
    },
    disabled: {
      description: "Whether the toggle is disabled.",
      defaultValue: false,
      control: {
        type: "boolean",
      },
    },
  }}
/>

export const Template = (args) => <Toggle {...args} />;

# Toggle

`Toggle` is a component that can be used to toggle between two states.

<Actions packageName="toggle" />

<Story name="example">
  <div className="mb-8">
    <Toggle id="unique-id" />
  </div>
</Story>

## Import

```bash
import { Toggle } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
