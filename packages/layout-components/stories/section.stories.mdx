import {Section} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Layout Components/Section"
  component={Section}
  argTypes={{
    as: {
      control: {
        defaultValue: 'section',
        type: 'select',
        options: ['div', 'section', 'main', 'article', 'header', 'footer'],
      },
    },
  }}
/>

export const Template = args => <Section {...args}>Hello World</Section>

# Section

`Section` is a container that provides a consistent layout for your content.

<Actions packageName="layout-components" />

<Story name="example">
  <div className="mb-8">
    <Section />
  </div>
</Story>

## Import

```bash
import { Section } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
