import { Container } from "../lib";
import { Grid } from "@daren/grid";
import { Actions } from "../../../storybook/components/actions";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

<Meta
  title="Layout Components/Container"
  component={Container}
  argTypes={{
    size: {
      control: {
        defaultValue: "default",
        type: "select",
        options: ["small", "medium", "default", "full"],
      },
    },
    as: {
      control: {
        defaultValue: "div",
        type: "select",
        options: ["div", "section", "main", "article", "header", "footer"],
      },
    },
  }}
/>

export const Template = (args) => (
  <Grid nested>
    <Container {...args}>Hello World</Container>
  </Grid>
);

# Container

`Container` is a component that should be used within a `Grid` and provides a centered div with a specific width.

<Actions packageName="layout-components" />

<Story name="example">
  <div className="mb-8">
    <Container />
  </div>
</Story>

## Import

```bash
import { Container } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
