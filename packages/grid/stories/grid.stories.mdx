import { Grid } from "../lib";
import { Actions } from "../../../storybook/components/actions";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

<Meta
  title="Layout Components/Grid"
  component={Grid}
  argTypes={{
    as: {
      control: {
        defaultValue: "Grid",
        type: "select",
        options: ["div", "section", "main", "article", "header", "footer"],
      },
    },
    nested: {
      defaultValue: false,
      control: {
        type: "boolean",
      },
    },
  }}
/>

export const Template = (args) => (
  <Grid {...args}>
    <div className="col-span-full">col-span-full</div>
    <div className="col-span-4">col-span-4</div>
    <div className="col-span-4">col-span-4</div>
    <div className="col-span-4">col-span-4</div>
  </Grid>
);

# Grid

`Grid` is a container that provides a consistent layout for your content.

<Actions packageName="grid" />

## Import

```bash
import { Grid } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
