import {Alert} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Components/Alert"
  component={Alert}
  argTypes={{
    type: {
      description: 'The type of alert to display',
      defaultValue: 'info',
      control: {
        type: 'select',
        options: ['success', 'info', 'warning', 'danger'],
      },
    },
    description: {
      description: 'The description of the alert',
      defaultValue: 'This is a description',
      control: {
        type: 'text',
      },
    },
    title: {
      description: 'The title of the alert',
      defaultValue: 'A title',
      control: {
        type: 'text',
      },
    },
    hideIcon: {
      description: 'Whether or not to hide the icon',
      defaultValue: false,
      control: {
        type: 'boolean',
      },
    },
  }}
/>

export const Template = args => <Alert {...args} />

# Alert

Darenui uses the `Alert` component to display a message to the user. It can be
used to inform the user of a change, or to display a message of success. There
are four types of alerts: `success`, `info`, `danger` and `warning`.

<Actions packageName="alert" />

<Story name="info">
  <div className="mb-8">
    <Alert
      description="This is a description of an alert"
      title="Alert title"
      type="info"
    />
  </div>
</Story>

<Story name="warning">
  <div className="mb-8">
    <Alert
      description="This is a description of an alert"
      title="Alert title"
      type="warning"
    />
  </div>
</Story>

<Story name="success">
  <div className="mb-8">
    <Alert
      description="This is a description of an alert"
      title="Alert title"
      type="success"
    />
  </div>
</Story>

<Story name="danger">
  <div className="mb-8">
    <Alert
      description="This is a description of an alert"
      title="Alert title"
      type="danger"
    />
  </div>
</Story>

## Import

```bash
import { Alert } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
