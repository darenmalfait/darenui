import {Modal} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Components/Modal"
  component={Modal}
  argTypes={{
    type: {
      description: 'Type of modal',
      control: {
        type: 'select',
        options: ['danger', 'warning', 'success', 'info'],
      },
    },
    title: {
      description: 'Title of modal',
      defaultValue: 'This is a title',
      control: {
        type: 'text',
      },
    },
    description: {
      description: 'Description of modal',
      defaultValue: 'This is a description and can be anything you want',
      control: {
        type: 'text',
      },
    },
    actions: {
      description: 'The bottom part of the modal',
      defaultValue: 'Can be any component',
      control: {
        type: 'text',
      },
    },
    initialFocus: {
      description: 'The element to focus on when the modal is opened',
      control: {
        type: null,
      },
    },
  }}
/>

export const Template = args => {
  const [open, setOpen] = React.useState(false)
  return (
    <>
      <button
        className="p-2 rounded-md bg-secondary text-primary text-sm focus-ring"
        onClick={() => setOpen(true)}
      >
        Open Modal
      </button>
      <Modal open={open} onClose={() => setOpen(false)} {...args} />
    </>
  )
}

# Modal

A `Modal` component is a Modal window that can be used to display content to the
user.

<Actions packageName="modal" />

## Import

```bash
import { Modal } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
