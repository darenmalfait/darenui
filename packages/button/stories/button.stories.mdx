import {Button} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Components/Button/Button"
  component={Button}
  argTypes={{
    as: {
      control: {
        defaultValue: 'a',
        type: 'select',
        options: ['button', 'section', 'main', 'article', 'header', 'footer'],
      },
    },
    size: {
      control: {
        defaultValue: 'medium',
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
    variant: {
      control: {
        defaultValue: 'primary',
        type: 'select',
        options: ['primary', 'secondary', 'danger', 'success'],
      },
    },
  }}
/>

export const Template = args => <Button {...args}>button</Button>

# Button

`Button` is a basic button component

<Actions packageName="button" />

## Import

```bash
import { Button } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
