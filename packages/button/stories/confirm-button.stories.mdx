import {ConfirmButton} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Components/Button/ConfirmButton"
  component={ConfirmButton}
  argTypes={{
    confirmString: {
      defaultValue: 'app-name',
      control: {
        type: 'text',
      },
    },
    size: {
      control: {
        defaultValue: 'medium',
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
    variant: {
      control: {
        defaultValue: 'primary',
        type: 'select',
        options: ['primary', 'secondary', 'danger', 'success'],
      },
    },
  }}
/>

export const Template = args => (
  <ConfirmButton {...args}>ConfirmButton</ConfirmButton>
)

# ConfirmButton

`ConfirmButton` is a button component that requires the user to type a string to
confirm the action.

<Actions packageName="button" />

## Import

```bash
import { ConfirmButton } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
