import {LinkButton} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Components/Button/LinkButton"
  component={LinkButton}
  argTypes={{
    as: {
      control: {
        defaultValue: 'a',
        type: 'select',
        options: ['button', 'section', 'main', 'article', 'header', 'footer'],
      },
    },
  }}
/>

export const Template = args => <LinkButton {...args}>LinkButton</LinkButton>

# LinkButton

`LinkButton` is a button that looks like a link

<Actions packageName="button" />

## Import

```bash
import { LinkButton } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
