import {Link} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Components/Button/Link"
  component={Link}
  argTypes={{
    as: {
      control: {
        defaultValue: 'a',
        type: 'select',
        options: ['Link', 'section', 'main', 'article', 'header', 'footer'],
      },
    },
    href: {
      control: {
        defaultValue: 'https://google.com',
        type: 'text',
      },
    },
    external: {
      control: {
        defaultValue: false,
        type: 'boolean',
      },
    },
  }}
/>

export const Template = args => <Link {...args}>Link</Link>

# Link

`Link` is a basic Link component

<Actions packageName="Link" />

## Import

```bash
import { Link } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
