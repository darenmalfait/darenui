import {DeleteConfirmButton} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Components/Button/DeleteConfirmButton"
  component={DeleteConfirmButton}
  argTypes={{
    confirmString: {
      defaultValue: 'app-name',
      control: {
        type: 'text',
      },
    },
    size: {
      control: {
        defaultValue: 'medium',
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
    variant: {
      control: {
        defaultValue: 'primary',
        type: 'select',
        options: ['primary', 'secondary', 'danger', 'success'],
      },
    },
  }}
/>

export const Template = args => (
  <DeleteConfirmButton {...args}>DeleteConfirmButton</DeleteConfirmButton>
)

# DeleteConfirmButton

`DeleteConfirmButton` is a button component that requires the user to type a
string to confirm the action.

<Actions packageName="button" />

## Import

```bash
import { DeleteConfirmButton } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
