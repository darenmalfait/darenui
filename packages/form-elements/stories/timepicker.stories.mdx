import {TimePicker} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'

<Meta
  title="Form Elements/TimePicker/TimePicker"
  component={TimePicker}
  argTypes={{
    name: {
      defaultValue: 'timepicker-id',
      control: {
        type: 'text',
      },
    },
  }}
/>

export const Template = args => <TimePicker {...args} />
export const TemplateWithIcon = args => <TimePicker {...args} />

# TimePicker

`TimePicker` is a dropdown field with search functionality

<Actions packageName="form-elements" />

<Story name="example">
  <div className="mb-8">
    <TimePicker name="timepicker" />
  </div>
</Story>

## Import

```bash
import { TimePicker } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />
