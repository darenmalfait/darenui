import {Checkbox} from '../lib'
import {Actions} from '../../../.storybook/components/actions'
import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs'
import {CheckIcon, PlusIcon} from '@heroicons/react/solid'

<Meta
  title="Form Elements/Checkbox/Checkbox"
  component={Checkbox}
  argTypes={{
    variant: {
      defaultValue: 'sm',
      control: {
        type: 'select',
        options: ['sm', 'md', 'lg'],
      },
    },
    bgClassName: {
      defaultValue: 'text-success',
      control: {
        type: 'text',
      },
    },
    textClassName: {
      defaultValue: 'text-white',
      control: {
        type: 'text',
      },
    },
    icon: {
      control: {
        type: null,
      },
    },
  }}
/>

export const Template = args => <Checkbox {...args} />

# Checkbox

`Checkbox` is a simple checkbox component.

<Actions packageName="form-elements" />

<Story name="example">
  <div className="mb-8 flex space-x-2">
    <Checkbox />
    <Checkbox bgClassName="text-red-500" icon={PlusIcon} />
    <Checkbox
      className="rotate-45"
      bgClassName="text-yellow-400"
      icon={PlusIcon}
    />
  </div>
</Story>

## Import

```bash
import { Checkbox } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />

### Custom icon

<Canvas>
  <Story name="custom-icon">
    <Checkbox checked icon={PlusIcon} />
  </Story>
</Canvas>
