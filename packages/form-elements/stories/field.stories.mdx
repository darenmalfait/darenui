import { Field } from "../lib";
import { Actions } from "../../../storybook/components/actions";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { CheckIcon, SearchIcon } from "@heroicons/react/solid";

<Meta
  title="Form Elements/Field"
  component={Field}
  argTypes={{
    error: {
      defaultValue: "",
      control: {
        type: "text",
      },
    },
    type: {
      defaultValue: "text",
      control: {
        type: "select",
        options: [
          "text",
          "textarea",
          "password",
          "email",
          "number",
          "search",
          "tel",
          "url",
        ],
      },
    },
    label: {
      defaultValue: "input label",
      control: {
        type: "text",
      },
    },
    name: {
      defaultValue: "input-id",
      control: {
        type: "text",
      },
    },
  }}
/>

export const Template = (args) => <Field label="input label" {...args} />;
export const TemplateWithIcon = (args) => (
  <Field label="input label" icon={SearchIcon} {...args} />
);

# Field

`Field` is a simple input component with label and information.

<Actions packageName="form-elements" />

<Story name="example">
  <div className="mb-8">
    <Field label="input label" name="first" />
  </div>
</Story>

## Import

```bash
import { Field } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />

### Custom icon

<Canvas>
  <Story name="custom-icon">{TemplateWithIcon.bind({})}</Story>
</Canvas>

<ArgsTable story="custom-icon" />
