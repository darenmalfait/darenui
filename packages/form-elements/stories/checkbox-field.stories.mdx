import { CheckboxField } from "../lib";
import { Actions } from "../../../storybook/components/actions";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { CheckIcon, PlusIcon } from "@heroicons/react/solid";

<Meta
  title="Form Elements/Checkbox/CheckboxField"
  component={CheckboxField}
  argTypes={{
    error: {
      defaultValue: "",
      control: {
        type: "text",
      },
    },
    variant: {
      defaultValue: "sm",
      control: {
        type: "select",
        options: ["sm", "md", "lg"],
      },
    },
    bgClassName: {
      defaultValue: "text-success",
      control: {
        type: "text",
      },
    },
    textClassName: {
      defaultValue: "text-white",
      control: {
        type: "text",
      },
    },
    icon: {
      control: {
        type: null,
      },
    },
    label: {
      defaultValue: "checkbox label",
      control: {
        type: "text",
      },
    },
    name: {
      defaultValue: "checkbox-id",
      control: {
        type: "text",
      },
    },
  }}
/>

export const Template = (args) => <CheckboxField {...args} />;

# CheckboxField

`CheckboxField` is a checkbox component with a label.

<Actions packageName="form-elements" />

<Story name="example">
  <div className="mb-8 flex space-x-2">
    <CheckboxField name="first" label="checkbox label" />
    <CheckboxField
      name="second"
      label="checkbox label"
      bgClassName="text-red-500"
      icon={PlusIcon}
    />
    <CheckboxField
      name="third"
      label="checkbox label"
      bgClassName="text-yellow-400"
      icon={PlusIcon}
    />
  </div>
</Story>

## Import

```bash
import { CheckboxField } from '@daren/ui-components'
```

## Usage

<Canvas>
  <Story name="usage">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="usage" />

### Custom icon

<Canvas>
  <Story name="custom-icon">
    <CheckboxField
      label="checkbox label"
      name="with-custom-icon"
      checked
      icon={PlusIcon}
    />
  </Story>
</Canvas>
